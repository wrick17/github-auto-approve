{
  "name": "github-auto-approve",
  "version": "1.0.0",
  "description": "Chrome Extension to automatically approve a PR on click of a button.",
  "main": "content.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@biomejs/biome": "^2.2.0"
  }
}
